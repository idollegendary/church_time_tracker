# Backend .env example
# Copy to backend/.env and edit values for your environment

# Async SQLAlchemy URL (asyncpg for Postgres)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/trecker_time

# JWT secret used to sign tokens - replace with a strong random value
JWT_SECRET=replace_with_a_strong_random_value

# Admin login to grant admin role on registration (optional)
ADMIN_LOGIN=admin@example.com

# Token lifetime in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Comma-separated list of allowed origins for CORS (no spaces)
# Example: http://localhost:5173,http://127.0.0.1:5173
ALLOW_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Example environment variables
# Local Postgres (outside docker)
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/trecker_time
# When running via docker-compose use the service host `db`:
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/trecker_time
JWT_SECRET=replace_me
